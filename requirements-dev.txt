# Dependances de dev / CI uniquement. A installer via :
#   pip install -r requirements-dev.txt
# en complement de backend/requirements.txt.

pytest==8.3.4
pytest-xdist==3.6.1
pytest-randomly==3.16.0
pytest-cov==6.0.0
hypothesis==6.122.3
pytest-split==0.9.0

# Mutation testing (G1 du doc AI_STRATEGY.md). Utilise par le workflow
# nightly `.github/workflows/mutation-testing.yml` ET utilisable localement
# (~3 min sur le tier 1 actuel — voir .claude/CLAUDE.md section "Mutation
# testing local"). Pas requis pour les tier 1/2/3 de la CI PR.
# Target : backend/app/logic/ (fonctions pures).
# Duree CI : 15-30 min (overhead Docker / setup runner). Seuil : 80 %.
mutmut==2.5.0
