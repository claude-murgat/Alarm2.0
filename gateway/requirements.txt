# Versions epinglees pour reproductibilite CI.
# Si tu mets a jour : ajuster aussi backend/requirements.txt si la dep est commune
# (ex: requests doit rester aligne entre les deux).

requests==2.31.0
pyserial==3.5
numpy==1.26.4
