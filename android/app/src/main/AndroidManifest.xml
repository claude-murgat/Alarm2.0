<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- INV-ANDROID-305 (2026-05-26) : détection "réseau totalement perdu" -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- READ_PHONE_STATE = runtime permission Android 6+ ; sans elle, le
         NetworkAvailabilityMonitor fait fail open (cellularInService=true) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- INV-ANDROID-308 (2026-06-02) : recevoir les SMS de commande
         "[ALARME-MURGAT-WAKE]" envoyés par le backend via gateway SIM7600
         pour déclencher la sonnerie locale "hors connexion". Runtime
         permission Android 6+, demandée au login dans MainActivity. -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:allowBackup="true"
        android:label="Alarme Murgat"
        android:theme="@style/Theme.AlarmeMurgat"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DashboardActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize" />

        <activity
            android:name=".AlarmActivity"
            android:exported="false"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:showWhenLocked="true"
            android:launchMode="singleTop" />

        <service
            android:name=".service.AlarmPollingService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

        <service
            android:name=".service.AlarmFirebaseService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- INV-ANDROID-308 : recevoir les SMS de commande wake du backend.
             Receiver static (manifest) → reste actif même si le foreground
             service AlarmPollingService a été tué par l'OS. -->
        <receiver
            android:name=".service.SmsWakeReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
